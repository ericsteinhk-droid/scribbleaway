# Core — required
python-docx>=1.1.0
openpyxl>=3.1.0

# Optional — enables named-entity recognition (people, orgs, locations)
# After installing spacy, also run:
#   python -m spacy download en_core_web_sm
spacy>=3.7.0
