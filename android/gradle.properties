# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Absolute path to a checkout of whisper.cpp. Populated by scripts/fetch-deps.sh
# (locally) or the CI workflow. Consumed by lib/src/main/jni/whisper/CMakeLists.txt
# via the -DWHISPER_DIR argument in lib/build.gradle.
# whisperDir=/absolute/path/to/whisper.cpp
